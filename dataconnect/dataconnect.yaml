specVersion: "v1beta"
serviceId: "Network"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "Network-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
